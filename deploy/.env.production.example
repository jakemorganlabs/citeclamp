# citeclamp production environment.
# Copy this file to .env.production and fill in each value. The repo holds only this example.
# The service binds 127.0.0.1 and reads the port from config/server.json. PORT is reserved for later sessions.

PORT=__REPLACE_ME__
HMAC_SECRET=__REPLACE_ME__
